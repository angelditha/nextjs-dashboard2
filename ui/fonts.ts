import { Lusitana } from 'next/font/google'; // or wherever you're importing it from

export const lusitana = Lusitana({
  subsets: ['latin'], // or appropriate subsets
  weight: ['400', '700'], // or appropriate weights
});